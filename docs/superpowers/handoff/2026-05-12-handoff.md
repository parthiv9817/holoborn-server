# 2026-05-12 Handoff — late-night pipeline validation session

**Written 2026-05-12 ~01:25** after Parthiv finished real-key backend testing and logged off.
**Pickup target:** Tuesday morning, full frontend day per agreed plan.
**Demo target:** Wednesday 2026-05-13 morning.

---

## Read in this order on pickup

1. **`mistakes/2026-05-11.md`** — section 7 (duck hour). Behavioral rule for any Unity work: direct YAML/JSON edits, NOT click-by-click.
2. **`memory/project_demo_wednesday_target.md`** — the locked target.
3. **`memory/project_runpod_demo_warmup_playbook.md`** — NEW. 30-min pre-demo warmup ritual to avoid 9-min cold-start.
4. **`diaries/2026-05-11.md`** — full Monday + late-night log.
5. **`docs/superpowers/specs/2026-05-11-stage-1-premium-design.md`** — Stage 1 UI spec.

---

## What got validated tonight (post-10pm pickup)

✅ **Real-key end-to-end pipeline VALIDATED:**
- Burst-frame upload → OpenAI portraitizer (56.9s, real call) → RunPod TRELLIS (8.6 min) → Meshy Retexture (2.0 min) → clean GLB delivered to `/avatars/{task_id}.glb`. Total 12.1 min.
- Cost: ~$0.30 (OpenAI + RunPod + Meshy combined).
- Warm-worker projection: ~7-8 min total.

✅ **Critical parser bug found + fixed in `app/services/meshy_client.py`:**
- `extract_glb_url()` was reading `task.result.model_urls.glb` (nested) when Meshy returns `model_urls.glb` at the TOP LEVEL of the GET response. Verified against [Meshy retexture docs](https://docs.meshy.ai/en/api/retexture).
- Symptom: Meshy retexture succeeded silently (10 credits consumed), parser threw, pipeline gracefully fell back to TRELLIS plastic. **User got TRELLIS plastic GLBs for what looked like a working chain.** Catastrophic if undetected pre-demo.
- Fix: read `model_urls` at top level. Also added `task.get("model_url")` (singular) as fallback.
- Live in code.

✅ **`hd_texture=True` enabled in production pipeline** (`generation_pipeline.py` submit_retexture call):
- Pro plan unlocks 4K base color textures.
- NOT yet validated via real test — tomorrow's first run is the validation.

✅ **`TEST_PORTRAIT_OVERRIDE` disabled in `.env`:**
- Real OpenAI portraitizer now in production path.
- Bypass code preserved (no-op when env var unset) for offline dev.

✅ **RunPod min_workers playbook documented** for demo days.

## Real retextured GLB available for inspection

`tests/outputs/full_pipeline/91161109_RETEXTURED.glb` (7.9 MB, valid glTF, downloaded direct from Meshy via curl after parser bug discovery). This is the ACTUAL retextured output Parthiv asked for, not the TRELLIS plastic the pipeline returned tonight before the fix.

## Open quality question (Parthiv flagged)

Web-UI Meshy retexture (free tier) produced visibly better output than our API retexture (Pro plan). Same inputs (TRELLIS GLB + portraitized image). Hypotheses ranked:

1. **`enable_original_uv=True` keeps TRELLIS's spatially-hashed auto-UVs.** Web UI likely defaults to `false` → Meshy regenerates proper UV unwrap → better texture mapping. ★ Most likely fix.
2. **`enable_pbr=True` produces multi-map PBR output that Babylon.js / casual viewers under-render** → looks flat / plastic in those viewers even though the data is correct. Test by opening in a PBR-aware viewer (Unity, gltf.report).
3. Standard-res output (1024²) vs HD (4096²) — `hd_texture=True` already addresses, awaiting validation.

**Action on pickup:**
- (a) Parthiv likely got ChatGPT image-analysis feedback overnight (master prompt was drafted and given to him).
- (b) Run ONE tomorrow-morning warm pipeline test with `hd_texture=True` AND optionally `enable_original_uv=False`. Compare to tonight's `91161109_RETEXTURED.glb`. If quality jumps to web-UI parity → ship it. If not → escalate to Meshy support / try art_style variants.

## Tomorrow (Tuesday) — full frontend day per Parthiv's plan

### Quest required (Parthiv brings headset back from home → office)

1. **Backend warmup ritual** (per `project_runpod_demo_warmup_playbook.md`): bump `workersMin=1`, fire one HD-on pipeline run, validate output quality.
2. **Stage 1 UI on-device verify:**
   - Press Y on left controller → fires `_DebugStatusSimulator.BeginRitual` → arrival plays
   - Open issue: **torso missing on mannequin** (multi-renderer/multi-submesh material replacement didn't fix it). Debug path in `docs/superpowers/handoff/2026-05-11-late-evening-handoff.md` — try vanilla URP/Lit material first to isolate (mesh vs shader).
3. **End-to-end real-pipeline test:** X-button burst capture → real backend → clean GLB → Stage 1 arrival → reveal at end. ~7-8 min if worker warm.
4. **Demo capture** for Vipin: `adb pull` the headset MP4.
5. **Cleanup:** lanyard off for capture per `project_demo_lanyard_removal.md`.

### Quest-not-required (any time)

- Refine `enable_original_uv` based on overnight ChatGPT analysis + Parthiv's web-UI A/B
- Re-test pipeline if quality knobs change
- Polish Stage 1 timings (timing values in `AnatomicalMannequinController` Inspector are tunable without code changes)

## Tooling notes for tomorrow's session

- **Screenshots:** macOS Cmd+Shift+4 (no Ctrl) saves to `~/Desktop/` which I CAN Read. Paths from clipboard-only screenshots live in `/var/folders/.../NSIRD_screencaptureui_*` — sandbox-blocked from my process (`Operation not permitted`). Ask Parthiv to use Cmd+Shift+4 not Cmd+Shift+Ctrl+4 for any screenshots he wants me to actually see.
- **uvicorn:** auto-reload picks up Python file changes, but **not .env changes**. Restart uvicorn after any `.env` edit.
- **RunPod GraphQL mutation:** see `project_runpod_demo_warmup_playbook.md` for the full required fields.

## State of services at handoff

- uvicorn: running on :8000 (background task, may have been killed if terminal closed — Parthiv can restart with `.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload`)
- ngrok: running, tunnel `https://grinning-flyable-golf.ngrok-free.dev` → :8000
- RunPod `workersMin`: **0** (reset after testing). Tomorrow's warmup ritual sets it to 1.
- Backend `.env`: real OpenAI key, real Meshy key, RUNPOD_API_KEY present, TEST_PORTRAIT_OVERRIDE commented out.

## What to NOT do tomorrow

- Don't suggest adding Meshy `/rigging` to the production pipeline as if it's new work — Parthiv already validated rigging+animation via `tests/scripts/test_meshy_manual.py` on Saturday's session. Treat it as a separate Quest-triggered flow.
- Don't walk Parthiv through Unity Editor click-by-click. Edit YAML/JSON directly. (See section 7 of `mistakes/2026-05-11.md`.)
- Don't propose scope cuts to premium Stage 1 visuals. Vipin values product > deadline per `feedback_no_timeline_pressure_framing.md`.

## One final note for the next session

Backend is done. The Wednesday demo blocker is now **frontend-only**: get Stage 1 visuals correct on Quest (torso bug + on-device tuning). Worst-case timeline: tonight's work + tomorrow's full Quest day = Wednesday morning demo viable. Parthiv earned that runway by pushing through real-key validation despite hitting parser bug + cold-start failure + 1 AM fatigue.
